# SWE.5 結合テスト / SWE.6 適格性確認テスト

## SWE.5 結合テスト
コンポーネント（SWA）をまたぐ振る舞いを確認する。単一ファイルでは
「UI → ロジック → 永続化 → 再描画」の一連の流れが対象。

| ITC | 結合シナリオ | 関係するSWA | 期待 |
|-----|--------------|-------------|------|
| ITC-01 | 入力→保存→リロード→復元 | SWA-UI, SWA-STORE | 再描画後も残る |

## SWE.6 適格性確認テスト（受け入れ）
SWR の受け入れ基準を、実ブラウザ E2E（`04_test/qualification/`）で確認する。

| QTC | 対象SWR | 手順 | 期待結果 |
|-----|---------|------|----------|
| QTC-XXX-01 | SWR-XXX-01 | 記録ボタンを押す | 一覧に1件増える／リロードで残る |
| QTC-XXX-02 | | | |
| QTC-REG-01 | SWR-CORE-01 | 既存機能を一通り操作 | 回帰なし |

## 実行
`04_test/README.md` を参照（Playwright + Chromium）。
Pass 件数と終了コードで合否を判定する。
